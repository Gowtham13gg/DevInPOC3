# Devin Apps Deployment Configuration
app:
  name: sf-lead-api
  type: spring-boot
  port: 8080

# Disable Nginx Basic Authentication
nginx:
  auth:
    enabled: false
    
# Allow public access to API endpoints
security:
  public_paths:
    - /api/app/insert/SFLead/details
